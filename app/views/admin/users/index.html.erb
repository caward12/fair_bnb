<h3> All Users </h3>

<%= @users.each do |user| %>
    <%= link_to user.full_name, user_path(user) %> / <%= user.email %> /
    <% if user.active? %>
      Active
    <% else %>
      Inactive
    <% end %>
    <br>

  <%end %>