My Properties<br>

<% @properties.each do |property| %>
<%= link_to property.name, property_path(property.id) %>    <%= link_to "View Reservations(#{property.reservations.count})", user_property_reservations_path(property.id) %><br>
<% end %>
